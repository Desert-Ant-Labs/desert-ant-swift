import SwiftUI

/// Desert Ant Labs design tokens, mirrored from `desert-ant-labs/design-system`.
///
/// The CSS files in `design-system/tokens/` are the source of truth. When the
/// brand changes, change those first, then update the values here.
///
/// Usage:
///
///     Text("Eye")
///         .font(DS.Font.display(38))
///         .foregroundStyle(DS.Color.textPrimary)
///         .padding(DS.Space.s5)
///
public enum DS {}
